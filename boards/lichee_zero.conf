# NES Emulator - lichee_zero board-specific overrides
#
# Board defconfig already provides:
#   HEAP (4MB), SDHC, DISPLAY, I2C (touch), CACHE, GPIO, PWM (backlight)
# No additional board-level overrides needed.
# All app config is in prj.conf.
